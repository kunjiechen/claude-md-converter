# Markdown转Word/PDF转换器依赖

# Markdown解析
markdown-it-py>=2.0.0
mdit-py-plugins>=0.3.0

# Word文档生成
python-docx>=0.8.11

# PDF生成（推荐weasyprint，样式保真度高；缺系统库时会自动降级）
weasyprint>=59.0

# PDF低保真兜底（可选但建议安装）
reportlab>=4.0.0

# 图片处理
Pillow>=9.0.0

# HTML模板引擎
Jinja2>=3.0

# HTML解析（Word导出器使用）
beautifulsoup4>=4.12

# 开发依赖（可选）
pytest>=7.0.0
black>=22.0.0
flake8>=4.0.0
